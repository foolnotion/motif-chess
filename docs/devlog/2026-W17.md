# Week 17 — 2026-04-20

## Decisions

- **[D001] Add Epic 2 performance track (stories 2-6, 2-7, 2-8):** During story 2-5
  implementation and profiling, three distinct bottleneck layers were identified: upstream
  chesslib SAN resolution, pgnlib PGN materialization, and the downstream SQLite/DuckDB
  storage path. Rather than absorbing this into the current story or opening a new epic,
  a sprint change proposal was approved to extend Epic 2 with three focused stories.
  Rejected: rolling back completed work; creating a new epic (premature); allocator-first
  redesign (not yet justified without upstream wins first).

- **[D002] Drop story 2-7 (pgnlib import_stream integration):** `pgnlib::import_stream`
  silently discards variations, NAGs, and annotations — it solves the wrong problem for a
  database that must preserve full PGN fidelity. If the parser ever proves a real
  bottleneck, the correct approach is a lexy callback-based action grammar, developed
  independently outside the sprint. Rejected: proceeding with import_stream (data loss);
  immediate lexy grammar work (premature without profiling evidence).

- **[D003] DuckDB C API only (`<duckdb.h>`) — no C++ API:** DuckDB's own documentation
  marks the C++ API as internal: "It is not guaranteed to be stable and can change without
  notice. If you would like to build an application on DuckDB, we recommend using the C
  API." The Clang 21 incompatibility discovered in Epic 1 (incomplete type in
  `profiling_utils.hpp`) is a consequence of using an unstable internal API. All DuckDB
  code uses `<duckdb.h>` exclusively (`duckdb_database`, `duckdb_connection`,
  `duckdb_appender`). This applies to all future stories. Rejected: C++ API (explicitly
  unsupported for applications); C++ wrapper shim (unnecessary indirection over a stable
  C interface).

- **[D004] Use legality-preserving stateful SAN replay in import paths:** The direct
  `chesslib` vs `chess-library` SAN corpus benchmark initially showed a large replay-path
  speedup, but that result was invalid because the new `san::replayer` fast path skipped
  legality checking for single-candidate moves. After restoring legality checks upstream,
  the stateful path still matched `chesslib::san::from_string` on final positions and
  remained slightly faster on the 10k corpus (`453ms` vs `465ms` with verification on 5
  rounds). Because PGN import naturally consumes consecutive SAN moves on a rolling board,
  motif-chess should use the stateful API in its import loops before any further full-path
  profiling. Rejected: keeping the stateless parser in import despite a matching stateful
  API; treating the pre-fix replay benchmark as valid optimization evidence.

- **[D005] Make SQLite authoritative during import; rebuild DuckDB afterward:** Profiling
  showed the live SQLite+DuckDB write path was the wrong shape. Importing into SQLite first,
  then rebuilding the derived DuckDB position store in bulk, cut the 10k corpus from
  `11.82s` to `6.20s`, and later to `3.54s` once index creation was delayed until after the
  load. Rejected: preserving live DuckDB writes during import (too much coordination and
  checkpoint/index overhead); optimizing the old pipeline before changing the data flow.

- **[D006] Default to serial import until parallelism proves its value again:** With the
  old taskflow shape, `pipeline` mode (`12.69s`) was slightly slower than forced serial
  (`12.62s`) on the 10k fixture while using more CPU. The default import config now favors
  a single worker and one in-flight line; future parallelism should be reintroduced only at
  coarse batch boundaries. Rejected: keeping fine-grained per-game taskflow parallelism as
  the default despite measured regressions.

- **[D007] Delay DuckDB position index creation until after bulk rebuild:** Rebuilding the
  position table with the Zobrist index already present still burned time and memory. Moving
  `CREATE INDEX` after the bulk load cut 10k end-to-end rebuild flow from `6.17s` to
  `3.54s`, and 3.4M from `6:34` to `5:08`. Rejected: maintaining the index during append or
  rebuilding it incrementally.

- **[D008] Cache repeated metadata IDs and reuse SQLite prepared statements:** PGN corpora
  repeat player, event, and tag names constantly. Adding in-memory ID caches plus prepared
  statement reuse in `game_store` brought the optimized path from roughly `5:39` on 1M to
  `1:50`, comfortably under the `2:00` perf gate. Rejected: further micro-tuning SAN parsing
  before fixing the repeated SQLite metadata work.

## Learned

- **NFR03 gap is 11×:** Post-2.6 baselines on 10k games: SAN-only `0.78s`, full import
  with deferred position index `13.48s`. Extrapolating to 10M games gives ~3.7 hours
  against a 20-minute target. Since position indexing is deferred in the slow baseline,
  the bottleneck lives in the SQLite game-store write path, not DuckDB.

- **DuckDB Appender is the key insert primitive at scale:** [aix](https://github.com/thomas-daniels/aix)
  (DuckDB chess extension over 7 billion Lichess games) uses `duckdb_appender` for all
  bulk inserts. Per-row SQL `INSERT` would be orders of magnitude slower. If
  `position_store::insert_batch` is not already using the appender, that is the prime
  Task 3 candidate for story 2-8.

- **scoutfish confirms the lexy direction:** [scoutfish](https://github.com/mcostalba/scoutfish)
  parses PGN with a two-table FSM (`ToToken[256]`, `ToStep[11][16]`) — one byte per table
  lookup, no heap allocation, L1-resident. This is exactly the callback-based action
  grammar approach for any future parser work. scoutfish's 16-bit move encoding is also
  identical to the CPW convention used by `chesslib::codec` — no change needed there.

- **Parallel byte-range scan eliminates a position index at moderate scale:** aix runs
  full-database heatmap queries over 7 billion games in 92–241 seconds on 24 threads with
  no pre-built position index. At 10M games that scales to ~130–340ms per query — well
  within NFR01's 100ms P99 budget if we use parallel scanning rather than a Zobrist hash
  index. Worth validating against our DuckDB schema before investing in index tuning.

- **`game_store::insert` always returns `error_code::duplicate` on conflict** — the
  `skip_duplicates` option struct was removed in the 1.3 code review. All callers,
  including the import worker, must treat duplicate as skip-and-log, not fatal.

- **`CONVENTIONS.md` is the cross-tool authority:** Epic 1 retro identified that rules
  written only to `CLAUDE.md` do not reach Copilot or OpenCode agents. `CONVENTIONS.md`
  is the single source of truth; all tool config files reference it.

- **The large replay benchmark win was a correctness bug, not a real optimization:** Once
  legality checks were restored in `chesslib::san::replayer`, the SAN-only gap between the
  stateful and stateless `chesslib` paths shrank to noise-level territory. The remaining
  meaningful performance gap is versus `chess-library`, which points to representation-level
  limits in the current `0x88` design rather than an unexploited parser shortcut.

- **The 10k fixture contains 18 duplicate identities, not parse failures:** Diagnostic runs
  consistently reported `attempted=10000`, `committed=9982`, `skipped=18`, `errors=0`.
  The apparent shortfall in imported games is expected under the current duplicate policy.

- **SAN parsing is no longer the bottleneck:** On the optimized path, 10k end-to-end timing
  for `san::replayer` (`1.10s`) and `san::from_string` (`1.06s`) was effectively identical.
  On the 3.4M corpus both modes clustered around `6m49s–6m51s` when run under the same
  architecture, confirming that SAN choice is now a secondary concern.

- **The main remaining risk is memory, not throughput:** The optimized indexed 3.4M run
  reached `~21.6 GB` RSS while finishing in `6:34`. Skipping index creation after rebuild
  dropped RSS to `~3.0 GB` and improved elapsed time to `5:08`, strongly implicating index
  construction as the current memory dealbreaker.

## Done

- **Epic 1 complete:** 5 stories, 52 tests passing, 0 ASan/UBSan violations, 18 code
  review findings resolved across 4 stories. Foundation: SQLite game store, DuckDB
  position schema, database manager, schema lifecycle, manifest, scratch base.

- **Stories 2-1 through 2-6 complete:**
  - 2-1: spdlog async logger infrastructure
  - 2-2: PGN reader / pgnlib adapter
  - 2-3: import worker — single-game processing unit
  - 2-4: parallel import pipeline with checkpoint/resume (taskflow DAG)
  - 2-5: import completion summary and error logging
  - 2-6: upstream chesslib SAN optimization adopted
    (revision `97fd1b5679a4c0871b5b3a40b441e81645fd9770`)
  - post-2-6 follow-up: legality-preserving `san::replayer` merged into motif-chess import
    paths; fresh full-import profiling pending

- **Story 2-7 dropped** and sprint status updated with rationale recorded.

- **Story 2-8 created** (`ready-for-dev`) with full prior art context (aix, scoutfish),
  NFR03 gap analysis, DuckDB C API appender pattern, and deferred items from 2-6 code
  review.

- **Import path reworked and benchmarked repeatedly:**
  - live serial SQLite+DuckDB write path: `11.82s` on 10k
  - SQLite import + DuckDB rebuild: `6.20s`
  - delayed index creation after rebuild: `3.54s`
  - metadata caches + statement reuse: `1.08s` on 10k, `1:50` on 1M
  - current 3.4M benchmark with index: `6:34`
  - current 3.4M benchmark without index creation: `5:08`

- **SQLite import path optimized:** `game_store` now caches player/event/tag IDs and reuses
  prepared statements for hot metadata and game insert operations.

- **Perf scaffolding improved:** import perf tests now support corpus override via
  `MOTIF_IMPORT_PERF_PGN`, explicit serial vs pipeline comparisons, SQLite-only runs,
  rebuild-only runs, and SAN mode comparisons.

## Problems and Solutions

- **Problem:** pgnlib `import_stream` loses variations, NAGs, and annotations — integrating
  it into the pipeline would silently degrade PGN fidelity with no visible error.
  **Solution:** dropped story 2-7; documented the lexy callback-based grammar as the
  correct future approach if profiling ever shows the parser as a bottleneck.

- **Problem:** Story 2-8 initially included a `duckdb::Appender` C++ code example, which
  is invalid under Clang 21. **Solution:** corrected to `duckdb_appender` C API pattern
  before the story was handed off.

- **Problem:** NFR03 (10M games in <20 minutes) appears unreachable at current throughput
  (11× gap). **Solution:** story 2-8 profiles to find the dominant bottleneck before
  committing to a fix; prime hypothesis is that `position_store::insert_batch` is using
  individual SQL INSERTs rather than `duckdb_appender`.

- **Problem:** The initial taskflow import pipeline looked parallel on paper but did not beat
  serial execution in practice. **Solution:** made serial the default, then rebuilt the
  architecture around SQLite-first import and bulk DuckDB rebuild before reconsidering any
  new parallel design.

- **Problem:** Bulk import throughput improved dramatically, but memory exploded during the
  3.4M indexed rebuild (`~21.6 GB` RSS). **Solution:** ran a no-index rebuild variant;
  memory dropped to `~3.0 GB`, showing the position index build is the primary RSS spike.

- **Problem:** Large benchmark gains looked suspicious because imported row counts were below
  the corpus filename. **Solution:** added a diagnostic summary test and validated that the
  18 missing games on the 10k fixture are duplicate skips, not correctness regressions.

## Open Questions

- What is the best low-RSS replacement for the current global Zobrist index build?
  Two concrete candidates should be tested first: partitioned/partial indexes (ECO is the
  most obvious partition key) and sorted insertion that relies on DuckDB zonemap pruning
  rather than an ART index.

- Can index creation be made optional or backgrounded in product UX without harming the most
  common queries? The no-index benchmark is compelling, but query latency without the index
  still needs validation.

- Is there any coarse-grained parallel design that beats the current serial baseline without
  reintroducing per-game coordination overhead? If so, it should likely parallelize whole
  batches or phases, not taskflow tokens per game.

- How close can the current architecture get to the `scidb` baseline on the 3.4M corpus
  before a more radical storage redesign (binary game blobs, aix-style layouts) becomes
  justified?

- Epic 2 retrospective is still marked optional. Worth running after this optimization arc
  stabilizes so the final import architecture and memory tradeoffs are recorded before Epic 3.

## Optimization Backlog

- **P0 — Replace ART index experiment with sorted table + zonemaps:** Highest-priority
  memory investigation. Measure rebuild time, peak RSS, and representative position lookup
  latency on a `position` table ordered by `zobrist_hash` without an explicit ART index.

- **P0 — Query benchmark on no-index dataset:** Before committing to any index strategy,
  benchmark representative position lookups against the current no-index rebuild output to
  see whether explicit indexing is even necessary for the main workloads.

- **P1 — ART index memory mitigation:** If an explicit index is still needed, test
  `DETACH/ATTACH` after index creation, then `memory_limit` + `temp_directory` during index
  build, to determine whether peak RSS can be made operationally acceptable.

- **P1 — Coarse-grained parallelism revisit:** Revisit taskflow only after the serial
  baseline is stable. Future parallelism should be around coarse batch or phase boundaries
  (SQLite import, DuckDB rebuild, index build), not per-game SERIAL->PARALLEL->SERIAL
  handoff.

- **P1 — 3.4M comparison baseline:** Keep using `/home/bogdb/scid/twic/pgn/all.pgn` as the
  large-corpus benchmark for external comparisons (e.g. scidb, aix-style storage ideas).
  Record future runs against this same corpus for consistency.

- **P2 — SAN parsing mode:** `san::replayer` and `san::from_string` are now near-parity.
  Treat SAN parsing as a solved secondary concern unless a future architecture change makes
  move decoding dominant again.

- **P2 — Metadata path follow-ups:** Player/event/tag ID caches and prepared statement reuse
  delivered large wins. Future SQLite-side work, if needed, should focus on statement reuse
  gaps or higher-level transaction batching only after memory/index decisions settle.

- **P3 — Partitioned/partial index design:** If zonemaps alone are insufficient, explore
  partition-aware indexing strategies. ECO code is the most obvious first partition key, but
  game-id range and opening-prefix hashes are also candidates.

- **P3 — Alternative storage models as control benchmarks:** Aix/binary-blob style storage is
  still worth benchmarking as a reference point, but not as an immediate implementation
  target. Use it to calibrate how much headroom remains in the current architecture.
